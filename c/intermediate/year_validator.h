#include <stdio.h>
void main()
{   char pref;
    int dd,mm,year;

    printf("What format do you prefer? DD-MM-YYYY(A)MM-DD-YYYY(B) OR YYYY-MM-DD(I)\n Enter A,B,I"):
    scanf("%c",&pref);
    if
    if((dd>0 && dd<32)&&(mm>0 && mm<13)&&(year>0 && year<9999))
 {
    if(mm==1 || mm==3||mm==5||mm==8||mm==10||mm==12)
    {
        if(dd==31)
        {
            printf("")
        }
    }
 }


} 
